#include <user/user.h>
#include <kernel/types.h>
#include <kernel/stat.h>
#include <kernel/proc.h>


int main(){

    //format -> proc name(proc id): proc state
    
    acquire(&ptable.lock);



    exit(0);
}